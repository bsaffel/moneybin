# Web app UI kit

`index.html` — the dashboard home (dark, 1440), a static reference extraction of `MoneyBin Brand Kit.dc.html` §09. Pixel values here are the source of truth; the live/interactive version (chart crosshair, SQL toggle) is the DC file itself.

Screens worth adding next: Transactions (full table), SQL console, Ask (MCP chat), Settings/vault, onboarding/unlock. The mobile app and marketing site have no kit yet.
