# Web app UI kit

`index.html` — the dashboard home (dark, 1440), a static reference extraction of the brand kit's §09 dashboard mockup. Pixel values here are the source of truth; the live/interactive version (chart crosshair, SQL toggle) lives in the claude.ai Design Kit project.

Screens worth adding next: Transactions (full table), SQL console, Ask (MCP chat), Settings/vault, onboarding/unlock. The mobile app and marketing site have no kit yet.
