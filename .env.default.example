# MoneyBin Configuration - Default Profile
# Copy this file to .env.default and configure with your credentials
# Or create separate profiles: .env.alice, .env.bob, etc.

# ============================================================================
# Plaid API Configuration
# ============================================================================
# Get your credentials from: https://dashboard.plaid.com/team/keys
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret

# Environment: sandbox (test), development, or production (real accounts)
PLAID_ENV=sandbox

# ============================================================================
# Database Configuration
# ============================================================================
# Path to the DuckDB database file
# Leave commented to use default: data/duckdb/moneybin.duckdb
# For separate databases per user: data/duckdb/moneybin_alice.duckdb
# DUCKDB_PATH=data/duckdb/moneybin.duckdb

# ============================================================================
# Data Processing Configuration
# ============================================================================
# Number of days to look back when extracting transactions
# MONEYBIN_PLAID__DAYS_LOOKBACK=365

# Batch size for Plaid API requests (max 500)
# MONEYBIN_PLAID__BATCH_SIZE=500

# ============================================================================
# Profile Usage Examples
# ============================================================================
# Create separate config files for each user:
#   .env.alice    - Alice's personal accounts
#   .env.bob      - Bob's personal accounts
#   .env.household - Shared household accounts
#   .env.dev      - Testing with Plaid sandbox
#   .env.prod     - Production real account data
#
# Use with CLI:
#   moneybin --profile=alice extract plaid
#   moneybin --profile=bob load parquet
#   moneybin --profile=household transform run
#
# ============================================================================
# Security Notes
# ============================================================================
# - Never commit .env files to version control (they are gitignored)
# - Each profile has separate credentials and transaction data
# - Credentials are never passed on command line (secure)
# - Use PLAID_ENV=sandbox for testing before connecting real accounts
# - Consider separate databases per user via DUCKDB_PATH
