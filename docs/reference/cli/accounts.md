<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin accounts

Account listing, settings, and lifecycle ops

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin accounts [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin accounts list`](#moneybin-accounts-list) | List accounts. |
| [`moneybin accounts summary`](#moneybin-accounts-summary) | Summarize account counts, lifecycle state, and recent activity. |
| [`moneybin accounts get`](#moneybin-accounts-get) | Show one account's full settings + dim record. |
| [`moneybin accounts set`](#moneybin-accounts-set) | Update account settings (structural + behavioral fields). |
| [`moneybin accounts resolve`](#moneybin-accounts-resolve) | Resolve a free-text account reference to ranked account_id candidates. |
| [`moneybin accounts balance`](#moneybin-accounts-balance) | Balance assertions, history, and reconciliation |
| [`moneybin accounts links`](#moneybin-accounts-links) | Review and manage account-link binding decisions |

## moneybin accounts list

List accounts. Hides archived accounts by default.

Usage: `moneybin accounts list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |
| `--include-archived` | flag |  | Include archived accounts in the listing |
| `--type` | text |  | Filter by account_type (canonical: depository, credit, loan, investment, other) or account_subtype (source detail: checking, savings, credit card, ...); case-insensitive |

## moneybin accounts summary

Summarize account counts, lifecycle state, and recent activity.

Usage: `moneybin accounts summary [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts get

Show one account's full settings + dim record.

Usage: `moneybin accounts get [OPTIONS] ACCOUNT_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `ACCOUNT_ID` | text | yes | Account ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts set

Update account settings (structural + behavioral fields).

Structural: --official-name, --last-four, --subtype, --holder-category,
--currency, --credit-limit, --default-cost-basis-method (each clearable
via --clear-FIELD). --default-cost-basis-method must be one of fifo,
hifo, specific, average — an invalid value is rejected before any write.
Behavioral: --display-name, --include/--exclude, --archive/--unarchive.
Archive cascades --exclude in the same write; unarchive does NOT restore
include. At least one field flag required.

Usage: `moneybin accounts set [OPTIONS] ACCOUNT_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `ACCOUNT_ID` | text | yes | Account ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--official-name` | text |  | Institution's formal account name |
| `--last-four` | text |  | Last 4 digits of account number |
| `--subtype` | text |  | Plaid-style account subtype (e.g., checking, savings, credit card) |
| `--holder-category` | text |  | Account holder type (personal/business/joint) |
| `--currency` | text |  | ISO-4217 currency code (e.g., USD) |
| `--credit-limit` | text |  | Credit limit (for credit cards / lines) |
| `--display-name` | text |  | Custom display name override (use --clear-display-name to clear) |
| `--default-cost-basis-method` | text |  | Per-account cost-basis default: fifo, hifo, specific, or average (NULL falls back to the global FIFO default) |
| `--include / --exclude` | flag |  | Include or exclude this account from net worth |
| `--archive / --unarchive` | flag |  | Archive (cascades --exclude) or unarchive (does not auto-restore include) |
| `--clear-official-name` | flag |  |  |
| `--clear-last-four` | flag |  |  |
| `--clear-subtype` | flag |  |  |
| `--clear-holder-category` | flag |  |  |
| `--clear-currency` | flag |  |  |
| `--clear-credit-limit` | flag |  |  |
| `--clear-display-name` | flag |  |  |
| `--clear-default-cost-basis-method` | flag |  |  |
| `--yes, -y` | flag |  | Skip soft-validation prompt for non-canonical values |

## moneybin accounts resolve

Resolve a free-text account reference to ranked account_id candidates.

Fuzzy-matches against display_name, account_subtype, and institution_name.
Use this before commands that need an account_id when you only have a
natural-language reference.

Usage: `moneybin accounts resolve [OPTIONS] QUERY`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `QUERY` | text | yes | Free-text account reference (e.g., 'my Chase account') |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit, -n` | int (≥ 1) | `5` | Maximum number of candidates to return |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts balance

Balance assertions, history, and reconciliation

Usage: `moneybin accounts balance [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin accounts balance show`](#moneybin-accounts-balance-show) | Show current or as-of balances per account. |
| [`moneybin accounts balance history`](#moneybin-accounts-balance-history) | Per-account balance history (daily series). |
| [`moneybin accounts balance assert`](#moneybin-accounts-balance-assert) | Assert a balance for an account on a specific date. |
| [`moneybin accounts balance list`](#moneybin-accounts-balance-list) | List balance assertions, optionally filtered by account. |
| [`moneybin accounts balance assertion-delete`](#moneybin-accounts-balance-assertion-delete) | Delete a balance assertion. |
| [`moneybin accounts balance reconcile`](#moneybin-accounts-balance-reconcile) | Show observed balance days with non-zero reconciliation delta. |

## moneybin accounts balance show

Show current or as-of balances per account.

Usage: `moneybin accounts balance show [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |
| `--account` | text |  | Filter to a single account_id |
| `--as-of` | text |  | ISO date (YYYY-MM-DD); shows balance on or before |

## moneybin accounts balance history

Per-account balance history (daily series).

Usage: `moneybin accounts balance history [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--account` | text |  | Required. Account ID (required) |
| `--from` | text |  |  |
| `--to` | text |  |  |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts balance assert

Assert a balance for an account on a specific date.

Usage: `moneybin accounts balance assert [OPTIONS] ACCOUNT_ID ASSERTION_DATE AMOUNT`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `ACCOUNT_ID` | text | yes |  |
| `ASSERTION_DATE` | text | yes | ISO date (YYYY-MM-DD) |
| `AMOUNT` | text | yes | Balance amount as decimal |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--notes` | text |  |  |
| `--yes, -y` | flag |  |  |

## moneybin accounts balance list

List balance assertions, optionally filtered by account.

Usage: `moneybin accounts balance list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--account` | text |  |  |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts balance assertion-delete

Delete a balance assertion. Silent no-op if no row exists.

Usage: `moneybin accounts balance assertion-delete [OPTIONS] ACCOUNT_ID ASSERTION_DATE`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `ACCOUNT_ID` | text | yes |  |
| `ASSERTION_DATE` | text | yes | ISO date (YYYY-MM-DD) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  |  |

## moneybin accounts balance reconcile

Show observed balance days with non-zero reconciliation delta.

Usage: `moneybin accounts balance reconcile [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--account` | text |  |  |
| `--threshold` | text | `0.01` |  |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts links

Review and manage account-link binding decisions

Usage: `moneybin accounts links [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin accounts links pending`](#moneybin-accounts-links-pending) | List pending account-link decisions, grouped by provisional account. |
| [`moneybin accounts links set`](#moneybin-accounts-links-set) | Accept (merge) or standalone-reject a pending account-link decision. |
| [`moneybin accounts links history`](#moneybin-accounts-links-history) | Show recent account-link decisions (all statuses), newest first. |
| [`moneybin accounts links run`](#moneybin-accounts-links-run) | Propose account merges: sweep for duplicates, or link two accounts by id. |

## moneybin accounts links pending

List pending account-link decisions, grouped by provisional account.

Shows provisional accounts with candidate merge proposals. Each group
leads with the account's name and lists, per candidate, its name, match
signal, ledger overlap, and the ids to act on. Use `accounts links set` to
decide each group.

Usage: `moneybin accounts links pending [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts links set

Accept (merge) or standalone-reject a pending account-link decision.

Pass exactly one of:

```text
--into <candidate_account_id>   merge the provisional into the candidate
--standalone                    reject all candidates; provisional stays standalone
```

A merge shows what it moves and asks before committing; pass --yes to answer
it in advance. Rejecting is never gated — the provisional account stays where
it is.

Examples:

```console
moneybin accounts links set dec001 --into ACC002
moneybin accounts links set dec001 --into ACC002 --yes
moneybin accounts links set dec001 --standalone
```

Usage: `moneybin accounts links set [OPTIONS] DECISION_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `DECISION_ID` | text | yes | Decision ID to act on (from `accounts links pending`) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--into` | text |  | Merge: the candidate account_id to adopt (from the pending group) |
| `--standalone` | flag |  | Standalone-reject: keep the provisional account as its own canonical entity |
| `--yes, -y` | flag |  | Skip the merge confirmation prompt (--into only; --standalone never asks) |

## moneybin accounts links history

Show recent account-link decisions (all statuses), newest first.

Usage: `moneybin accounts links history [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit, -n` | int (≥ 0) | `50` | Max records to show |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin accounts links run

Propose account merges: sweep for duplicates, or link two accounts by id.

With no arguments, finds weak-signal candidate pairs for every account in
core.dim_accounts that has no pending proposal yet. Run this after importing
accounts from multiple sources to surface cross-source twins.

With two account ids, proposes exactly that pair — the escape hatch for a
duplicate no signal reaches, where nothing matches but you know it is one
account. Order is direction: the first id is absorbed and the second kept,
whenever both are absorbable. Neither form merges: both write proposals for
`accounts links set`.

Usage: `moneybin accounts links run [OPTIONS] [ACCOUNT_ID] [CANDIDATE_ACCOUNT_ID]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[ACCOUNT_ID]` | text | no | Account absorbed on accept (requires CANDIDATE_ACCOUNT_ID) |
| `[CANDIDATE_ACCOUNT_ID]` | text | no | Account kept on accept (requires ACCOUNT_ID) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
