<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin sync

Sync transactions from external services

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin sync [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin sync login`](#moneybin-sync-login) | Authenticate with moneybin-sync via Device Authorization Flow. |
| [`moneybin sync logout`](#moneybin-sync-logout) | Clear stored JWTs and any in-progress device authorization sessions. |
| [`moneybin sync link`](#moneybin-sync-link) | Link a bank account via Plaid (formerly: sync connect). |
| [`moneybin sync link-status`](#moneybin-sync-link-status) | Poll a sync link session for completion (formerly: sync connect-status). |
| [`moneybin sync disconnect`](#moneybin-sync-disconnect) | Remove a bank connection. |
| [`moneybin sync pull`](#moneybin-sync-pull) | Pull data from connected institutions. |
| [`moneybin sync status`](#moneybin-sync-status) | Show connected institutions, last sync times, and health. |

## moneybin sync login

Authenticate with moneybin-sync via Device Authorization Flow.

Usage: `moneybin sync login [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--no-browser` | flag |  | Print URL only; don't try to open a browser. |

## moneybin sync logout

Clear stored JWTs and any in-progress device authorization sessions.

Usage: `moneybin sync logout [OPTIONS]`

## moneybin sync link

Link a bank account via Plaid (formerly: sync connect).

Establishes a new Plaid-mediated link to an institution, or
re-authenticates one in error state. JSON output mode is event-driven:
returns the initiate response immediately so an agent can present the
link to the user and verify completion with `sync link-status` later.
Text mode blocks until the user finishes the Plaid flow in their
browser and returns the auto-pull summary.

Usage: `moneybin sync link [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--institution` | text |  | Re-authenticate this connected institution, or a label for a new one. |
| `--no-pull` | flag |  | Skip the auto-pull after connecting. |
| `--no-browser` | flag |  | Print URL only; don't try to open a browser. |
| `--yes, -y` | flag |  | Skip re-auth confirmation prompt. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin sync link-status

Poll a sync link session for completion (formerly: sync connect-status).

CLI mirror of MCP sync_link_status. Single-shot — returns whatever state
the server holds for `session_id` (pending, connected, or failed). Does
not poll; the caller decides when to check again.

Usage: `moneybin sync link-status [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--session-id` | text |  | Required. Session ID from link. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin sync disconnect

Remove a bank connection.

Usage: `moneybin sync disconnect [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--institution` | text |  | Required. Institution name to disconnect. |
| `--yes, -y` | flag |  | Skip confirmation prompt. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin sync pull

Pull data from connected institutions.

Usage: `moneybin sync pull [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--institution` | text |  | Sync specific institution by name. |
| `--force, -f` | flag |  | Reset cursor and re-fetch full history. |
| `--refresh / --no-refresh` | flag | `true` | Run the post-load refresh pipeline (matching + transforms + categorization) after a successful pull so core.* models (dim_accounts, etc.) reflect the new data before this command returns. Default: on. Pass --no-refresh to defer; transforms dominates pull latency, so high-frequency callers should defer and run refresh on a separate schedule. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin sync status

Show connected institutions, last sync times, and health.

Usage: `moneybin sync status [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |
| `--json-fields` | text |  | Comma-separated field projection (json output only). Available: id, provider_item_id, institution_name, provider, status, last_sync, error_code, guidance |
