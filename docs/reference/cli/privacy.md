<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin privacy

Privacy utilities: redaction and audit

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin privacy [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin privacy redact`](#moneybin-privacy-redact) | Apply the same PII-stripping used by export-uncategorized to any text. |
| [`moneybin privacy grant`](#moneybin-privacy-grant) | Grant consent to share \<category> data with an AI backend. |
| [`moneybin privacy revoke`](#moneybin-privacy-revoke) | Revoke consent for \<category>; takes effect immediately. |
| [`moneybin privacy revoke-all`](#moneybin-privacy-revoke-all) | Revoke ALL active AI consent grants. |
| [`moneybin privacy status`](#moneybin-privacy-status) | Show active AI consent grants, the configured backend, and consent policy. |
| [`moneybin privacy log`](#moneybin-privacy-log) | Show recent privacy log events (consent grants/revokes + tool calls). |

## moneybin privacy redact

Apply the same PII-stripping used by export-uncategorized to any text.

Useful for verifying what an LLM will see before exporting a full batch.

Examples:

```console
moneybin privacy redact "VENMO PAYMENT TO J SMITH"

echo "STARBUCKS #1234 SEATTLE WA" | moneybin privacy redact -
```

Usage: `moneybin privacy redact [OPTIONS] [DESCRIPTION]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[DESCRIPTION]` | text | no | Description to redact, or '-' to read from stdin. |

## moneybin privacy grant

Grant consent to share \<category> data with an AI backend.

Account numbers and other CRITICAL fields always remain masked.

Usage: `moneybin privacy grant [OPTIONS] CATEGORY`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `CATEGORY` | text | yes | Feature category, e.g. mcp-data-sharing |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--backend` | text |  | AI backend (defaults to MONEYBIN_AI__DEFAULT_BACKEND) |
| `--mode` | one of `persistent`, `one-time` | `persistent` | persistent or one-time |
| `--yes, -y` | flag |  | Skip the confirmation prompt |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin privacy revoke

Revoke consent for \<category>; takes effect immediately.

Usage: `moneybin privacy revoke [OPTIONS] CATEGORY`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `CATEGORY` | text | yes | Feature category to revoke |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--backend` | text |  | AI backend |
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin privacy revoke-all

Revoke ALL active AI consent grants.

Usage: `moneybin privacy revoke-all [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin privacy status

Show active AI consent grants, the configured backend, and consent policy.

Usage: `moneybin privacy status [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin privacy log

Show recent privacy log events (consent grants/revokes + tool calls).

Usage: `moneybin privacy log [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--last` | int | `50` | Max events to show (capped at 1000) |
| `--actor` | text |  | Filter by actor |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
