<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin transactions

Transactions and workflows on them (matches, categorize, review, notes, tags, splits, audit)

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin transactions [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions review`](#moneybin-transactions-review) | Pending counts across all review queues. |
| [`moneybin transactions create`](#moneybin-transactions-create) | Create a single manual transaction. |
| [`moneybin transactions audit`](#moneybin-transactions-audit) | List audit events for one transaction. |
| [`moneybin transactions list`](#moneybin-transactions-list) | List transactions with optional filters. |
| [`moneybin transactions categorize`](#moneybin-transactions-categorize) | Categorization workflow + rules (taxonomy under top-level `categories`) |
| [`moneybin transactions matches`](#moneybin-transactions-matches) | Review and manage transaction matches (dedup, transfers) |
| [`moneybin transactions notes`](#moneybin-transactions-notes) | Notes attached to transactions (multi-note threads) |
| [`moneybin transactions tags`](#moneybin-transactions-tags) | Tags applied to transactions (slug-flavored labels) |
| [`moneybin transactions splits`](#moneybin-transactions-splits) | Splits: allocate one transaction across categories |

## moneybin transactions review

Pending counts across all review queues.

DEPRECATED: use `moneybin review` instead. Removed after one minor release.

Usage: `moneybin transactions review [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--type` | text | `all` | all \| matches \| categorize \| account-links \| merchant-links \| security-links |
| `--status` | flag |  | Show queue counts (the default) |
| `--interactive` | flag |  | Walk the queue item by item (not yet built) |
| `--confirm` | text |  | Non-interactive: confirm one item by ID |
| `--reject` | text |  | Non-interactive: reject one item by ID |
| `--confirm-all` | flag |  | Non-interactive: confirm all items in scope |
| `--limit` | int | `50` | Cap items per session |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions create

Create a single manual transaction.

Usage: `moneybin transactions create [OPTIONS] AMOUNT DESCRIPTION`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `AMOUNT` | text | yes | Signed decimal amount (negative=expense) |
| `DESCRIPTION` | text | yes | Transaction description |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--account` | text |  | Required. Account ID |
| `--date` | text |  | Transaction date YYYY-MM-DD (defaults to today) |
| `--category` | text |  | Category name |
| `--subcategory` | text |  | Subcategory |
| `--merchant` | text |  | Merchant name |
| `--memo` | text |  | Memo text |
| `--note` | text |  | Free-form note (created as a separate note row) |
| `--tag` | text, repeatable |  | Tag (repeatable) |
| `--check-number` | text |  | Check number |
| `--payment-channel` | text |  | Payment channel (e.g., online, in_store) |
| `--currency` | text |  | ISO 4217 currency code |
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions audit

List audit events for one transaction.

Usage: `moneybin transactions audit [OPTIONS] TRANSACTION_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit` | int | `100` | Max events to return |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions list

List transactions with optional filters.

Usage: `moneybin transactions list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--account` | text, repeatable |  | Account ID or display name (repeatable). |
| `--from` | text |  | Start date ISO 8601, inclusive. |
| `--to` | text |  | End date ISO 8601, inclusive. |
| `--category` | text, repeatable |  | Category filter (repeatable). |
| `--amount-min` | text |  | Minimum amount as decimal string (e.g. '-50.00'). |
| `--amount-max` | text |  | Maximum amount as decimal string. |
| `--description` | text |  | ILIKE pattern against description and memo. |
| `--uncategorized` | flag |  | Only transactions with no user/AI/rule categorization assigned. |
| `--limit` | int | `50` | Maximum rows to return. |
| `--cursor` | text |  | Pagination token from previous call. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize

Categorization workflow + rules (taxonomy under top-level `categories`)

Usage: `moneybin transactions categorize [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions categorize export-uncategorized`](#moneybin-transactions-categorize-export-uncategorized) | Export uncategorized transactions as PII-scrubbed JSON for LLM review. |
| [`moneybin transactions categorize commit-from-file`](#moneybin-transactions-categorize-commit-from-file) | Commit LLM-generated categorizations from a JSON file to transactions. |
| [`moneybin transactions categorize pending`](#moneybin-transactions-categorize-pending) | List uncategorized transactions. |
| [`moneybin transactions categorize commit`](#moneybin-transactions-categorize-commit) | Commit externally-decided categorizations from a JSON array. |
| [`moneybin transactions categorize run`](#moneybin-transactions-categorize-run) | Run the categorization engine cascade over uncategorized transactions. |
| [`moneybin transactions categorize improve-ai`](#moneybin-transactions-categorize-improve-ai) | Re-categorize AI-guessed transactions to confident Plaid provider categories. |
| [`moneybin transactions categorize assist`](#moneybin-transactions-categorize-assist) | Return uncategorized transactions as PII-scrubbed records for LLM categorization. |
| [`moneybin transactions categorize stats`](#moneybin-transactions-categorize-stats) | Show categorization coverage summary. |
| [`moneybin transactions categorize rules`](#moneybin-transactions-categorize-rules) | Rule management (list, apply, create, delete, resolve conflicts) |
| [`moneybin transactions categorize auto`](#moneybin-transactions-categorize-auto) | Auto-rule proposal workflow |

## moneybin transactions categorize export-uncategorized

Export uncategorized transactions as PII-scrubbed JSON for LLM review.

Output is a JSON array of objects with the full ``RedactedTransaction``
shape — ``transaction_id``, ``description_scrubbed``, ``memo_scrubbed``,
``source_type``, plus structural signals (``transaction_type``,
``check_number``, ``is_transfer``, ``transfer_pair_id``,
``payment_channel``, ``amount_sign``). Merchant text is sent in full —
only embedded PII (e.g. account numbers) is masked. No amounts, dates, or
account identifiers. Feed the output to an LLM, fill in
category/subcategory, then pipe back through
``moneybin transactions categorize commit-from-file`` (extra export keys
are stripped at the commit boundary).

Usage: `moneybin transactions categorize export-uncategorized [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--output, -o` | path |  | Write JSON to this file path instead of stdout. |
| `--account-filter` | text, repeatable |  | Restrict to transactions in these account IDs (repeatable). |
| `--limit` | int (≥ 1) |  | Maximum transactions to export (defaults to configured assist_default_batch_size). |

## moneybin transactions categorize commit-from-file

Commit LLM-generated categorizations from a JSON file to transactions.

Reads a JSON array where each object has transaction_id, category, and
(optionally) subcategory.

Designed for the export → LLM → commit workflow:

```console
moneybin transactions categorize export-uncategorized -o todo.json
# edit todo.json: add category/subcategory to each item
moneybin transactions categorize commit-from-file todo.json
```

Or pipe through LLM tooling:

```console
moneybin transactions categorize export-uncategorized \
  | llm-tool --fill-categories \
  | moneybin transactions categorize commit-from-file -
```

Exit code is 1 if any item failed or was skipped.

Usage: `moneybin transactions categorize commit-from-file [OPTIONS] [INPUT_PATH]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[INPUT_PATH]` | path | no | Path to a JSON file produced by export-uncategorized, or '-' to read stdin. |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions categorize pending

List uncategorized transactions.

Excludes transfer pairs and archived accounts.

```console
moneybin transactions categorize pending
moneybin transactions categorize pending --sort impact
moneybin transactions categorize pending --min-amount 20 --output json
```

Usage: `moneybin transactions categorize pending [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit` | int | `50` | Maximum rows (default 50, max 1000). |
| `--sort` | text | `date` | Sort order: 'date' (most recent first) or 'impact' (ABS(amount)*age_days). |
| `--min-amount` | text | `0` | Filter to ABS(amount) >= this value. |
| `--account` | text |  | Filter to an account: accepts account_id or display_name (ambiguous matches error). |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize commit

Commit externally-decided categorizations from a JSON array.

Read from a file:

```console
moneybin transactions categorize commit --input cats.json
```

Or from stdin:

```console
cat cats.json | moneybin transactions categorize commit -
```

Per-item validation: failures are reported in the result without aborting
the batch. Exit code is 1 if any item failed.

Usage: `moneybin transactions categorize commit [OPTIONS] [STDIN_SENTINEL]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[STDIN_SENTINEL]` | text | no | Pass '-' to read JSON from stdin. |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--input` | text |  | Path to a JSON file with categorization items. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions categorize run

Run the categorization engine cascade over uncategorized transactions.

Engines available today: ``rules``, ``merchants``. Methods cascade in
order — a rule write blocks a merchant write at the same priority.

```console
moneybin transactions categorize run
moneybin transactions categorize run --methods rules
moneybin transactions categorize run --methods rules,merchants --output json
```

Usage: `moneybin transactions categorize run [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--methods` | text | `rules,merchants` | Comma-separated engines to run in order. Default: rules,merchants. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions categorize improve-ai

Re-categorize AI-guessed transactions to confident Plaid provider categories.

Reverse-looks-up every transaction currently categorized by
``categorized_by='ai'`` against the Plaid category bridge; upgrades it to
``provider_native`` only when the bridge match is at MEDIUM confidence or
higher. Only touches ``ai``-guessed rows — manual (``user``) and rule
(``rule``/``auto_rule``) categorizations are never overwritten. Prints the
count of transactions upgraded.

```console
moneybin transactions categorize improve-ai
moneybin transactions categorize improve-ai --output json
```

Usage: `moneybin transactions categorize improve-ai [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions categorize assist

Return uncategorized transactions as PII-scrubbed records for LLM categorization.

Outputs the same shape as the MCP tool transactions_categorize_assist: merchant
text (description/memo) is sent in full — it's the categorization signal — with
only embedded PII (e.g. account numbers) masked. No amount, date, or account
ID is included.

```console
moneybin transactions categorize assist --limit 50 --output json | jq '.data[0]'
moneybin transactions categorize assist --account-filter acct_a,acct_b --output json
```

Pipe the JSON output into an LLM workflow; commit decisions back via
`moneybin transactions categorize commit`.

Usage: `moneybin transactions categorize assist [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit` | int | `100` | Maximum number of records to return (default 100). |
| `--account-filter` | text |  | Comma-separated account IDs to restrict to. |
| `--date-range` | text |  | Date range as START,END (ISO dates, inclusive). |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions categorize stats

Show categorization coverage summary.

Usage: `moneybin transactions categorize stats [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize rules

Rule management (list, apply, create, delete, resolve conflicts)

Usage: `moneybin transactions categorize rules [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions categorize rules list`](#moneybin-transactions-categorize-rules-list) | Display all active categorization rules. |
| [`moneybin transactions categorize rules apply`](#moneybin-transactions-categorize-rules-apply) | Run all active rules against uncategorized transactions. |
| [`moneybin transactions categorize rules create`](#moneybin-transactions-categorize-rules-create) | Create one or more categorization rules. |
| [`moneybin transactions categorize rules delete`](#moneybin-transactions-categorize-rules-delete) | Soft-delete (deactivate) a categorization rule by ID. |
| [`moneybin transactions categorize rules list-conflicts`](#moneybin-transactions-categorize-rules-list-conflicts) | Show categorization rules refused because another rule owns the matcher. |
| [`moneybin transactions categorize rules resolve`](#moneybin-transactions-categorize-rules-resolve) | Resolve categorization rule conflicts. |

## moneybin transactions categorize rules list

Display all active categorization rules.

Usage: `moneybin transactions categorize rules list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize rules apply

Run all active rules against uncategorized transactions.

Usage: `moneybin transactions categorize rules apply [OPTIONS]`

## moneybin transactions categorize rules create

Create one or more categorization rules.

Single rule: pass NAME positionally with --pattern and --category.
Batch: pass --from-file pointing at a JSON list of rule dicts.

A 'contains' rule whose pattern is too short to discriminate is refused
unless --allow-broad is passed — see --allow-broad help.

Usage: `moneybin transactions categorize rules create [OPTIONS] [NAME]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[NAME]` | text | no | Rule name (omit when --from-file is used) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--pattern` | text |  | Merchant pattern to match |
| `--category` | text |  | Target category |
| `--subcategory` | text |  | Optional target subcategory |
| `--match-type` | one of `exact`, `contains`, `regex` |  | Pattern match strategy (default: contains) |
| `--priority` | int |  | Lower runs first (default: 100) |
| `--min-amount` | float |  |  |
| `--max-amount` | float |  |  |
| `--account-id` | text |  | Restrict to one account |
| `--from-file` | path |  | JSON file with a list of rule dicts |
| `--reapply` | flag |  | Apply newly-created rules to uncategorized rows after insert |
| `--allow-broad` | flag |  | Allow a 'contains' rule whose pattern is too short to discriminate (e.g. 'TO', which matches STORE/AUTO/TOTAL). Without this flag such rules are refused, not created. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize rules delete

Soft-delete (deactivate) a categorization rule by ID.

The rule remains in the database with is_active=false. Use --reapply to
strip categorizations written by this rule and re-evaluate those rows
against remaining active matchers.

Usage: `moneybin transactions categorize rules delete [OPTIONS] RULE_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `RULE_ID` | text | yes | Rule ID to deactivate (soft-delete) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--reapply` | flag |  | Re-evaluate transactions previously categorized by this rule |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize rules list-conflicts

Show categorization rules refused because another rule owns the matcher.

Usage: `moneybin transactions categorize rules list-conflicts [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize rules resolve

Resolve categorization rule conflicts.

Single conflict: pass CONFLICT_ID with exactly one of --replace,
--reprioritize N, or --cancel. Batch: pass --from-file pointing at a JSON
list of resolution dicts; the whole batch applies atomically or not at all.

A conflict recorded against a rule that has since been edited is refused
as stale — re-read the queue with `rules list-conflicts` and decide again.

Usage: `moneybin transactions categorize rules resolve [OPTIONS] [CONFLICT_ID]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[CONFLICT_ID]` | text | no | Conflict ID to resolve (omit when --from-file is used) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--replace` | flag |  | Deactivate the existing rule and activate the refused one |
| `--reprioritize` | int |  | Activate the refused rule beside the existing one at this priority (lower runs first) |
| `--cancel` | flag |  | Discard the refused rule and keep live state |
| `--from-file` | path |  | JSON file with a list of {"conflict_id", "resolution", "priority"} dicts |
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize auto

Auto-rule proposal workflow

Usage: `moneybin transactions categorize auto [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions categorize auto review`](#moneybin-transactions-categorize-auto-review) | List pending auto-rule proposals with sample transactions and trigger counts. |
| [`moneybin transactions categorize auto accept`](#moneybin-transactions-categorize-auto-accept) | Batch accept/reject auto-rule proposals. |
| [`moneybin transactions categorize auto stats`](#moneybin-transactions-categorize-auto-stats) | Show auto-rule health: active rules, pending proposals, transactions categorized. |
| [`moneybin transactions categorize auto rules`](#moneybin-transactions-categorize-auto-rules) | List active auto-rules (rules with created_by='auto_rule'). |

## moneybin transactions categorize auto review

List pending auto-rule proposals with sample transactions and trigger counts.

Usage: `moneybin transactions categorize auto review [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |
| `--limit` | int (≥ 1) |  | Maximum number of proposals to display (defaults to configured limit) |

## moneybin transactions categorize auto accept

Batch accept/reject auto-rule proposals.

Usage: `moneybin transactions categorize auto accept [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--accept` | text, repeatable |  | Proposal IDs to accept |
| `--reject` | text, repeatable |  | Proposal IDs to reject |
| `--accept-all` | flag |  | Accept all pending proposals |
| `--reject-all` | flag |  | Reject all pending proposals |
| `--allow-broad` | flag |  | Accept proposals flagged broad (match count far exceeds evidence). Review the estimated match count first — a broad rule recategorizes many transactions at once. |

## moneybin transactions categorize auto stats

Show auto-rule health: active rules, pending proposals, transactions categorized.

Usage: `moneybin transactions categorize auto stats [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions categorize auto rules

List active auto-rules (rules with created_by='auto_rule').

Usage: `moneybin transactions categorize auto rules [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |
| `--limit` | int (≥ 1) |  | Maximum number of auto-rules to display (defaults to configured limit) |

## moneybin transactions matches

Review and manage transaction matches (dedup, transfers)

Usage: `moneybin transactions matches [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions matches pending`](#moneybin-transactions-matches-pending) | List pending matches, grouped by component (copies of the same transaction cluster together). |
| [`moneybin transactions matches run`](#moneybin-transactions-matches-run) | Run matcher against existing transactions. |
| [`moneybin transactions matches history`](#moneybin-transactions-matches-history) | Show recent match decisions. |
| [`moneybin transactions matches undo`](#moneybin-transactions-matches-undo) | Reverse a match decision. |
| [`moneybin transactions matches set`](#moneybin-transactions-matches-set) | Accept or reject one pending match by id. |
| [`moneybin transactions matches backfill`](#moneybin-transactions-matches-backfill) | One-time scan of all existing transactions for latent duplicates. |

## moneybin transactions matches pending

List pending matches, grouped by component (copies of the same transaction cluster together).

Usage: `moneybin transactions matches pending [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--type` | text |  | Filter by match type: dedup or transfer |
| `--limit, -n` | int | `50` | Max records to show |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions matches run

Run matcher against existing transactions.

Usage: `moneybin transactions matches run [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--skip-transform` | flag |  | Skip transforms after matching |
| `--auto-accept-transfers` | flag |  | Auto-accept transfer matches (skip interactive review) |

## moneybin transactions matches history

Show recent match decisions.

Usage: `moneybin transactions matches history [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit, -n` | int | `20` | Max records to show |
| `--type` | text |  | Filter by match type: dedup or transfer |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions matches undo

Reverse a match decision.

Usage: `moneybin transactions matches undo [OPTIONS] MATCH_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `MATCH_ID` | text | yes | Match ID to reverse |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Skip confirmation |

## moneybin transactions matches set

Accept or reject one pending match by id.

Usage: `moneybin transactions matches set [OPTIONS] MATCH_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `MATCH_ID` | text | yes | Match ID to accept or reject |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--status` | text |  | Required. accepted or rejected |

## moneybin transactions matches backfill

One-time scan of all existing transactions for latent duplicates.

Usage: `moneybin transactions matches backfill [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--skip-transform` | flag |  | Skip transforms after matching |
| `--auto-accept-transfers` | flag |  | Auto-accept transfer matches (skip interactive review) |

## moneybin transactions notes

Notes attached to transactions (multi-note threads)

Usage: `moneybin transactions notes [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions notes add`](#moneybin-transactions-notes-add) | Add a new note to a transaction. |
| [`moneybin transactions notes list`](#moneybin-transactions-notes-list) | List all notes on a transaction. |
| [`moneybin transactions notes edit`](#moneybin-transactions-notes-edit) | Edit an existing note's text. |
| [`moneybin transactions notes delete`](#moneybin-transactions-notes-delete) | Delete a note. |

## moneybin transactions notes add

Add a new note to a transaction.

Usage: `moneybin transactions notes add [OPTIONS] TRANSACTION_ID TEXT`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |
| `TEXT` | text | yes | Note text |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions notes list

List all notes on a transaction.

Usage: `moneybin transactions notes list [OPTIONS] TRANSACTION_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions notes edit

Edit an existing note's text.

Usage: `moneybin transactions notes edit [OPTIONS] NOTE_ID TEXT`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `NOTE_ID` | text | yes | Note ID |
| `TEXT` | text | yes | New note text |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions notes delete

Delete a note.

Usage: `moneybin transactions notes delete [OPTIONS] NOTE_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `NOTE_ID` | text | yes | Note ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions tags

Tags applied to transactions (slug-flavored labels)

Usage: `moneybin transactions tags [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions tags add`](#moneybin-transactions-tags-add) | Apply one or more tags to a transaction. |
| [`moneybin transactions tags remove`](#moneybin-transactions-tags-remove) | Remove one or more tags from a transaction. |
| [`moneybin transactions tags list`](#moneybin-transactions-tags-list) | List tags on a transaction, or all distinct tags with usage counts. |
| [`moneybin transactions tags rename`](#moneybin-transactions-tags-rename) | Rename a tag globally (all transactions). |

## moneybin transactions tags add

Apply one or more tags to a transaction.

Usage: `moneybin transactions tags add [OPTIONS] TRANSACTION_ID TAGS...`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |
| `TAGS...` | text, repeatable | yes | One or more tags |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions tags remove

Remove one or more tags from a transaction.

Usage: `moneybin transactions tags remove [OPTIONS] TRANSACTION_ID TAGS...`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |
| `TAGS...` | text, repeatable | yes | One or more tags to remove |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions tags list

List tags on a transaction, or all distinct tags with usage counts.

Usage: `moneybin transactions tags list [OPTIONS] [TRANSACTION_ID]`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `[TRANSACTION_ID]` | text | no | Transaction ID (omit to list all distinct tags with usage counts) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions tags rename

Rename a tag globally (all transactions). Emits a parent audit event.

Usage: `moneybin transactions tags rename [OPTIONS] OLD NEW`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `OLD` | text | yes | Existing tag |
| `NEW` | text | yes | Replacement tag |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions splits

Splits: allocate one transaction across categories

Usage: `moneybin transactions splits [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin transactions splits add`](#moneybin-transactions-splits-add) | Append a split to a transaction. |
| [`moneybin transactions splits list`](#moneybin-transactions-splits-list) | List splits on a transaction. |
| [`moneybin transactions splits remove`](#moneybin-transactions-splits-remove) | Remove a single split. |
| [`moneybin transactions splits clear`](#moneybin-transactions-splits-clear) | Delete all splits on a transaction. |

## moneybin transactions splits add

Append a split to a transaction.

Usage: `moneybin transactions splits add [OPTIONS] TRANSACTION_ID AMOUNT`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Parent transaction ID |
| `AMOUNT` | text | yes | Signed decimal amount |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--category` | text |  | Category |
| `--subcategory` | text |  | Subcategory |
| `--note` | text |  | Optional split note |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions splits list

List splits on a transaction.

Usage: `moneybin transactions splits list [OPTIONS] TRANSACTION_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin transactions splits remove

Remove a single split.

Usage: `moneybin transactions splits remove [OPTIONS] SPLIT_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `SPLIT_ID` | text | yes | Split ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin transactions splits clear

Delete all splits on a transaction.

Usage: `moneybin transactions splits clear [OPTIONS] TRANSACTION_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `TRANSACTION_ID` | text | yes | Transaction ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Skip confirmation |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
