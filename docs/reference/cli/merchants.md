<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin merchants

Merchant mappings management

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin merchants [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin merchants list`](#moneybin-merchants-list) | List all merchant mappings. |
| [`moneybin merchants create`](#moneybin-merchants-create) | Create a merchant mapping. |
| [`moneybin merchants links`](#moneybin-merchants-links) | Review and manage merchant-link binding decisions |

## moneybin merchants list

List all merchant mappings.

Usage: `moneybin merchants list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin merchants create

Create a merchant mapping.

Usage: `moneybin merchants create [OPTIONS] PATTERN CANONICAL_NAME`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `PATTERN` | text | yes | Merchant name pattern |
| `CANONICAL_NAME` | text | yes | Canonical merchant name |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--default-category` | text |  | Default category for this merchant |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin merchants links

Review and manage merchant-link binding decisions

Usage: `moneybin merchants links [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin merchants links pending`](#moneybin-merchants-links-pending) | List pending merchant-link decisions, grouped by provider entity id. |
| [`moneybin merchants links set`](#moneybin-merchants-links-set) | Accept (bind) or reject a pending merchant-link decision. |
| [`moneybin merchants links history`](#moneybin-merchants-links-history) | Show recent merchant-link decisions (all statuses), newest first. |
| [`moneybin merchants links run`](#moneybin-merchants-links-run) | Harvest existing categorization facts into merchant-link bindings. |

## moneybin merchants links pending

List pending merchant-link decisions, grouped by provider entity id.

Shows provider entity ids with candidate merchant proposals. Each group
lists the candidate decision_id, merchant_id, canonical name, and
confidence. Use `merchants links set` to decide each group.

Usage: `moneybin merchants links pending [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin merchants links set

Accept (bind) or reject a pending merchant-link decision.

Pass exactly one of:

```text
--into <candidate_merchant_id>   bind the provider entity id to this merchant
--new                            reject; resolver mints a new merchant on next run
```

Examples:

```console
moneybin merchants links set dec001 --into merch0001aa
moneybin merchants links set dec001 --new
```

Usage: `moneybin merchants links set [OPTIONS] DECISION_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `DECISION_ID` | text | yes | Decision ID to act on (from `merchants links pending`) |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--into` | text |  | Accept: bind this provider entity id to the decision's candidate merchant_id (confirming safety check) |
| `--new` | flag |  | Reject all candidates; resolver mints a new merchant on its next categorization pass |

## moneybin merchants links history

Show recent merchant-link decisions (all statuses), newest first.

Usage: `moneybin merchants links history [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--limit, -n` | int (≥ 1) | `50` | Max records to show |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin merchants links run

Harvest existing categorization facts into merchant-link bindings.

Binds provider entity ids that point unambiguously to a single canonical
merchant (recorded immediately, no review), and routes one-id-many-merchant
conflicts to the pending review queue. Reports the two outcomes distinctly.

Run this after importing transactions with merchant_entity_id data to
surface binding decisions for review.

Usage: `moneybin merchants links run [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
