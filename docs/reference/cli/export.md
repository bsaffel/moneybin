<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin export

Export data to external formats

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin export [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin export bundle`](#moneybin-export-bundle) | Export the canonical portability bundle. |
| [`moneybin export report`](#moneybin-export-report) | Export one catalog report and typed parameter binding. |
| [`moneybin export destination`](#moneybin-export-destination) | List and manage saved export destinations |

## moneybin export bundle

Export the canonical portability bundle.

Usage: `moneybin export bundle [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--format` | one of `csv`, `parquet`, `xlsx` |  | Local artifact format: csv, parquet, or xlsx. Default: csv. |
| `--to` | text | `local:exports` | Explicit destination: local:\<name> or sheets:\<name>. |
| `--compress` | one of `zip` |  | Compress a CSV or Parquet local bundle as zip. |
| `--unredacted` | flag |  | Explicitly export unredacted data for this run only. |
| `--yes, -y` | flag |  | Accept the redacted default without prompting. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin export report

Export one catalog report and typed parameter binding.

Usage: `moneybin export report [OPTIONS] REPORT_ID`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `REPORT_ID` | text | yes | Stable catalog report ID. |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--param` | text, repeatable |  | Typed report parameter in key=value form; repeat for multiple values. |
| `--format` | one of `csv`, `parquet`, `xlsx` |  | Local artifact format: csv, parquet, or xlsx. Default: csv. |
| `--to` | text | `local:exports` | Explicit destination: local:\<name> or sheets:\<name>. |
| `--compress` | one of `zip` |  | Compress a CSV or Parquet local bundle as zip. |
| `--unredacted` | flag |  | Explicitly export unredacted data for this run only. |
| `--yes, -y` | flag |  | Accept the redacted default without prompting. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin export destination

List and manage saved export destinations

Usage: `moneybin export destination [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin export destination list`](#moneybin-export-destination-list) | List derived and saved export destinations with readiness. |
| [`moneybin export destination remove`](#moneybin-export-destination-remove) | Remove MoneyBin configuration without deleting destination content. |
| [`moneybin export destination add`](#moneybin-export-destination-add) | Add or replace a saved export destination |

## moneybin export destination list

List derived and saved export destinations with readiness.

Usage: `moneybin export destination list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress informational output (status lines, progress, ✅). |

## moneybin export destination remove

Remove MoneyBin configuration without deleting destination content.

Usage: `moneybin export destination remove [OPTIONS] NAME`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `NAME` | text | yes | Saved destination name or ID. |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Skip confirmation. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin export destination add

Add or replace a saved export destination

Usage: `moneybin export destination add [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin export destination add local`](#moneybin-export-destination-add-local) | Add or replace a local artifact destination. |
| [`moneybin export destination add sheets`](#moneybin-export-destination-add-sheets) | Authorize and add or replace a Google Sheets destination. |

## moneybin export destination add local

Add or replace a local artifact destination.

Usage: `moneybin export destination add local [OPTIONS] NAME PATH`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `NAME` | text | yes | Unique saved destination name. |
| `PATH` | path | yes | Local directory used for new artifacts. |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin export destination add sheets

Authorize and add or replace a Google Sheets destination.

Usage: `moneybin export destination add sheets [OPTIONS] NAME URL`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `NAME` | text | yes | Unique saved destination name. |
| `URL` | text | yes | Google Sheets workbook URL. |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
