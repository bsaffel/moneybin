<!-- Generated by scripts/generate_reference_docs.py from the Typer command tree. Do not edit; run `make generate-docs` after changing the source. -->
# moneybin system

System and data status

Generated from the registered command tree, so every option below is one the command declares; `--help` on any command prints the same text. Shared flags, exit codes, and JSON envelopes: [CLI reference](../../guides/cli-reference.md).

Usage: `moneybin system [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin system doctor`](#moneybin-system-doctor) | Run pipeline integrity checks across all invariants |
| [`moneybin system status`](#moneybin-system-status) | Show data inventory and pending review queue counts. |
| [`moneybin system audit`](#moneybin-system-audit) | Inspect the unified audit log |

## moneybin system doctor

Run pipeline integrity checks across all invariants

Usage: `moneybin system doctor [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--verbose, -V` | flag |  | Show every invariant that ran, not just the ones that need attention, plus the affected transaction IDs for each failing one. |
| `--full` | flag |  | Scan every protected app.* row for audit coverage instead of the sampled, recent-rows-only default. |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress optional status lines and progress; preserve results and recovery. |

## moneybin system status

Show data inventory and pending review queue counts.

Usage: `moneybin system status [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress optional status lines and progress; preserve results and recovery. |
| `--no-pager` | flag |  | Print the complete text result directly instead of opening a pager. |

## moneybin system audit

Inspect the unified audit log

Usage: `moneybin system audit [OPTIONS] COMMAND [ARGS]...`

**Commands**

| Command | Purpose |
|---|---|
| [`moneybin system audit list`](#moneybin-system-audit-list) | List audit events with filters. |
| [`moneybin system audit show`](#moneybin-system-audit-show) | Show one audit event plus any chained children (parent_audit_id matches). |
| [`moneybin system audit undo`](#moneybin-system-audit-undo) | Reverse every app.* mutation in one operation as a unit (keyed on operation_id). |
| [`moneybin system audit history`](#moneybin-system-audit-history) | List recent audited operations, newest first, with undoability. |
| [`moneybin system audit get`](#moneybin-system-audit-get) | Show full before/after for every row of one operation before undoing it. |

## moneybin system audit list

List audit events with filters.

Usage: `moneybin system audit list [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--actor` | text |  | Filter by actor |
| `--action` | text |  | Filter by action LIKE pattern (e.g., 'tag.%') |
| `--target-table` | text |  | Filter by target table |
| `--target-id` | text |  | Filter by target_id |
| `--from` | text |  | Filter occurred_at >= timestamp |
| `--to` | text |  | Filter occurred_at \<= timestamp |
| `--limit` | int | `100` | Max events to return |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress optional status lines and progress; preserve results and recovery. |
| `--no-pager` | flag |  | Print the complete text result directly instead of opening a pager. |

## moneybin system audit show

Show one audit event plus any chained children (parent_audit_id matches).

Usage: `moneybin system audit show [OPTIONS] {audit_id}`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `audit_id` | text | yes | Audit event ID |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `--no-pager` | flag |  | Print the complete text result directly instead of opening a pager. |

## moneybin system audit undo

Reverse every app.* mutation in one operation as a unit (keyed on operation_id).

Refuses (exit 1) when a later operation modified the same rows
(``undo_cascade_blocked`` — undo those first), when the operation was already
undone, when it touched a table outside the undoable app.* surface, or when
restoring the captured row would write back a value the write path no longer
admits (``undo_value_inadmissible`` — e.g. a pre-existing blank category).
The returned ``undo_operation_id`` is itself undoable.

Usage: `moneybin system audit undo [OPTIONS] {operation_id}`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `operation_id` | text | yes | Operation id to reverse |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--yes, -y` | flag |  | Confirm the displayed undo scope |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |

## moneybin system audit history

List recent audited operations, newest first, with undoability.

The pull surface for reversing a change when no error preceded the regret.

Usage: `moneybin system audit history [OPTIONS]`

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `--domain` | text |  | Filter to an action family (e.g. 'tag') |
| `--since` | text |  | Filter occurred_at >= timestamp |
| `--actor` | text |  | Filter by actor |
| `--limit` | int | `50` | Max operations to return |
| `--include-undone` | flag |  | Include the undo operations themselves |
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `-q, --quiet` | flag |  | Suppress optional status lines and progress; preserve results and recovery. |
| `--no-pager` | flag |  | Print the complete text result directly instead of opening a pager. |

## moneybin system audit get

Show full before/after for every row of one operation before undoing it.

Usage: `moneybin system audit get [OPTIONS] {operation_id}`

**Arguments**

| Argument | Type | Required | Description |
|---|---|---|---|
| `operation_id` | text | yes | Operation id to inspect |

**Options**

| Option | Type | Default | Description |
|---|---|---|---|
| `-o, --output` | one of `text`, `json` | `text` | Output format: 'text' (human-readable) or 'json' (machine-readable). |
| `--no-pager` | flag |  | Print the complete text result directly instead of opening a pager. |
