# Archived pre-cutover catalog: MoneyBin MCP

> **Archived 2026-07-19.** This document preserves the location and revision of
> the per-tool catalog that governed the former registry of 105 tools. It is
> historical reference only and must not be used as a current MCP contract.

The complete pre-cutover catalog is preserved in Git at the cutover revision:

```text
git show e7786e5a:docs/specs/moneybin-mcp.md
```

The active contract is [`../moneybin-mcp.md`](../moneybin-mcp.md); the frozen
baseline is [`../mcp-tool-surface-scaling.md`](../mcp-tool-surface-scaling.md).
