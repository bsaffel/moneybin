# Spec Index

Single source of truth for spec status. Update this table when a spec's status changes.

## Spec types

| Type | Purpose |
|---|---|
| **Umbrella** | High-level overview that defines the vision, pillars, and child specs for a major initiative. Not directly implementable — decomposes into Feature specs. |
| **Feature** | A single implementable unit of work with concrete schema, API, CLI, and test surface. The primary spec type. |
| **Architecture** | Cross-cutting design decisions, patterns, and contracts that multiple features depend on. |
| **Framework** | Policy and governance specs (privacy, security) that constrain how features are built rather than defining features themselves. |
| **Roadmap** | Strategic sequencing and phasing docs. Not implementable — inform prioritization and ordering of Feature specs. |

## Status definitions

| Status | Meaning |
|---|---|
| `draft` | Design written, not yet reviewed or ready for implementation |
| `ready` | Design reviewed and approved; ready for implementation |
| `in-progress` | Implementation underway |
| `implemented` | Shipped; spec stays in place as living documentation |

## Updating implemented features

| Change size | Action |
|---|---|
| **Bug fix / minor tweak** | No spec needed. Code change speaks for itself. |
| **Enhancement to existing feature** | New spec referencing the original as context. Original stays in place untouched. |
| **Full redesign / replacement** | New spec. Original gets a note at the top pointing to the replacement. Old spec moves to `archived/` since it's been superseded. |

## Cross-Surface

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Capabilities Map](moneybin-capabilities.md) | Architecture | draft | Cross-surface outcome map: one row per user-facing capability, one column per active surface (MCP, CLI, future REST). Single source of truth for "what user outcome reaches which surface, and under what registered name." Bootstrap rows demonstrate every coverage pattern; full population (~30–60 rows) lands incrementally. Referenced by the surface-change-discipline rule in `.claude/rules/mcp.md`. |

## Smart Import

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Overview](smart-import-overview.md) | Umbrella | ready | Six-pillar initiative: smart tabular detection, PDF, ML categorization, auto-rules, AI-assisted parsing |
| [Tabular Import](smart-import-tabular.md) | Feature | implemented | Universal tabular importer (CSV, TSV, Excel, Parquet, Feather); heuristic detection engine, multi-account support, migration formats (Tiller, Mint, YNAB, Maybe). Supersedes archived `csv-import` spec. |
| `migration-guides.md` | Feature | planned | Persona-shaped migration guide batch, not generic source-format docs. Guides should cover Mint, Tiller, YNAB/Actual, Maybe/Sure CSV, and OFX/QFX bank-download paths with the audience, what carries over, exact import flow, validation/doctor checks, lineage/export follow-through, and honest limitations. Public guides are gated on the corresponding import path being real; the Maybe/Sure guide must not claim shipped support until the `maybe` profile drift is resolved. |
| [Tabular Cleanup](smart-import-tabular-cleanup.md) | Feature | implemented | Post-ship cleanup: ResolvedMapping dataclass, Literal types, config params, DatabaseKeyError handler, Decimal correctness, N+1 merchant batch optimization, account matching wiring |
| [Smart Import Inbox](smart-import-inbox.md) | Feature | implemented | Watched-folder import UX: drop files in `~/Documents/MoneyBin/<profile>/inbox/`, run `moneybin import inbox` (or `import_inbox_sync` MCP), files move to `processed/YYYY-MM/` or `failed/YYYY-MM/` with YAML error sidecar. Per-profile lockfile + crash-recovery via staging-rename. |
| [Financial Format Import](smart-import-financial.md) | Feature | implemented | OFX/QFX/QBO parity with `smart-import-tabular.md`: shared `import_log` infrastructure, reversible imports, `Database.ingest_dataframe()` writes, magic-byte detection, institution resolution chain, formalized QBO support. Supersedes archived `ofx-import.md`. |
| [Transform Handoff](smart-import-transform.md) | Feature | implemented | Ships the four `transform_*` read MCP tools (`status`, `plan`, `validate`, `audit`) per `moneybin-mcp.md` §1530; `transform_apply` MCP tool subsequently folded into `refresh_run(steps=["transform"])` umbrella (PR #173). Renames `import_file` → `import_files` (batch-shaped, runs refresh pipeline at end of list); adds `system_status.transforms` block (pending/last_apply_at) so staleness is observable without a separate call. Closes the gap surfaced by the 2026-05-15 silent-stale-`dim_accounts` finding. |
| [Confirmation & Confidence](smart-import-confirmation.md) | Feature | in-progress | Cross-channel confirm-the-columns trust layer: one unified confidence contract (normalized score + tier) and one `import_preview`→`import_confirm` flow shared by tabular, gsheet, and PDF. First-encounter always confirms; confirmed layouts reuse silently; changes tabular `medium`-confidence from wave-through-with-warning to a gate; tiered agent autonomy (calibration-gated) paired with visible revert/re-map recovery. Owns the `import_confirm` signature + `resolve_or_confirm` primitive that `smart-import-pdf.md` references. |
| [PDF Import](smart-import-pdf.md) | Feature | implemented | Generic PDF import as an ingestion primitive — fuses Pillar C + the PDF slice of Pillar F. Deterministic-first ladder (`pdfplumber` → **bridge**: the agent already driving MoneyBin extracts via the cold-start export/apply seam) gated by W-2-style confidence scoring; **LLM-as-recipe-generator** (learned, user-vetted, deterministic replay) saved to `app.pdf_formats`. Two outcomes: transaction-shaped → `raw.tabular_transactions` (`source_type='pdf'`, no migration) → core; else → `raw.pdf_seeds` JSON + auto-generated views (connect-gsheet seed pattern, shared view-builder). **Bridge-only LLM in v1** — no separate AIBackend / in-process keys / ollama; honest egress to the user's own agent, no fake redaction (**amends `privacy-and-ai-trust.md`**). Scanned/image-only PDFs (no text layer) return an explicit unsupported outcome — a vision-capable backend is deferred (Out of Scope); no `pytesseract`. Successor to the cut W-2 extractor (#186). |
| `smart-import-ai-parsing.md` | Feature | planned | Pillar F: LLM fallback for file parsing. **PDF parsing folded into `smart-import-pdf.md`**; this spec, if written, covers non-PDF file types only. |

## Transaction Matching

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Overview](matching-overview.md) | Umbrella | implemented | Cross-source dedup, transfer detection, golden-record merge rules; core as gold analytics layer. Both v1 pillars shipped (PRs #43, #46, #47). |
| [Same-Record Dedup](matching-same-record-dedup.md) | Feature | implemented | Cross-source dedup + golden-record merge rules (pillars A+C); shared matching engine, `prep.int_transactions__matched`/`__merged`, `meta.fct_transaction_provenance`, `app.match_decisions`, `moneybin transactions matches run/history/undo/backfill` CLI plus `moneybin transactions review --type matches` |
| [Transfer Detection](matching-transfer-detection.md) | Feature | implemented | Transfer pair detection across accounts (pillar B); shared matching engine (Tier 4), `core.bridge_transfers`, always-review v1, 4-signal scoring |
| [N-Way Dedup](matching-nway-dedup.md) | Feature | implemented | Merge 3+ copies of the same transaction (enhances `matching-same-record-dedup.md`, supersedes its 1:1 assignment requirement). Union-find spanning-forest matcher, recursive-CTE prep fold, weakest-link (MIN) group confidence, component-grouped review UX, group-aware `dedup_reconciliation`. Pairwise schema unchanged; groups derived downstream. |
| [Exact-Key Dedup](matching-exact-key-dedup.md) | Feature | implemented | Cross-source exact-key auto-merge (M1B refinement): same account + exact amount + same day auto-merges regardless of description similarity (fixes OFX-truncation doubling, e.g. WF `.qfx`+`.csv` → 558 instead of 279). Confidence floor in `scoring.py` (description → tiebreaker); `(source_type, source_origin, source_file)` cardinality guard in `assign_components` pairs N true duplicates 1:1 to prevent over-collapse. Refines N-Way Req 2. |

## Categorization

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Overview](categorization-overview.md) | Umbrella | ready | Auto-rule generation, ML categorization, priority hierarchy, bootstrap strategies. Supersedes archived `transaction-categorization` spec. |
| [Auto-Rule Generation](categorization-auto-rules.md) | Feature | implemented | Auto-generate categorization rules from user edits (pillar E); merchant-first pattern extraction, proposal review queue |
| [Categorize Bulk](categorization-bulk.md) | Feature | implemented | `moneybin transactions categorize commit` CLI parity for `transactions_categorize_commit` MCP tool (both renamed from `bulk_apply` / `bulk` in PR #171); shared `CategorizationItem` Pydantic model with per-item validation; `RecordingContext` drops per-item duplicate DB lookups |
| [Cold Start](categorization-cold-start.md) | Feature | implemented | First-run categorization: LLM-assist workflow with type-enforced PII redaction, CLI export/apply bridge for agent + manual modes, `app.user_merchants` schema; renames `transactions_categorize_bulk_apply` → `_commit` (PR #171) and `auto_confirm` → `auto_accept`. Seed merchant catalogs retired 2026-05-15 — see spec amendment. |
| [Matching Mechanics](categorization-matching-mechanics.md) | Feature | implemented | Algorithm contract for the matcher: `match_text` (description + memo) construction, structural-field signals to LLM and matcher, `oneOf` exemplar accumulation replacing auto-generalized patterns, `categorized_by` source-priority enforcement on write, auto-apply (`categorize_pending`) on every `transactions_categorize_commit` commit, OP_SCORES specificity ranking |
| `categorization-ml.md` | Feature | planned | Pillar D: local ML-powered categorization |
| [Merchant Entity Resolution](merchant-entity-resolution.md) | Feature | implemented | M1T — the merchant twin of `account-identity-resolution.md`. Resolve a transaction's merchant by Plaid's stable `merchant_entity_id` **before** name matching. Two `app.*` tables mirroring the account pattern: `merchant_links` (provider-id → canonical `merchant_id` binding, provider-neutral `(source_type, ref_kind, ref_value)`, N:1) + `merchant_link_decisions` (fuzzy-match review queue). Adopt-or-mint ladder: adopt bound → auto-bind exact name → **review fuzzy** → mint `created_by='plaid'`. Provider id stays in `raw`/`prep` + `merchant_links`, never `core.fct_transactions`. Backfill harvests existing categorizations (zero review; conflicts-only). Identity only — category is Tier-2b's job. First concrete step of the broader first-class-merchant-entity vision. |
| [Categorization Source Model](categorization-source-model.md) | Feature | implemented | M1U — the category twin of the identity-MDM specs. Turns category assignment from a single winner-take-all golden record into a **source-aware model**: splits `categorized_by` into *method* vs a new `source_type` (aggregator) column on `app.transaction_categories` (`'plaid'` method → `'provider_native'`); keeps `categorized_by` = the *method* (merchant matches stamp `rule`; a provenance-aware demotion was built then reverted as inert and harmful to auto-rule health — provenance precision deferred) and lets a rule/merchant authored after the Plaid import override `provider_native` across runs; ships `apply_plaid_categories` (Plaid PFC codes → the **M1V category-source bridge**'s canonical `category_id`, two-tier detailed→primary reverse lookup, confidence-gated at ≥MEDIUM, run last-before-AI in `categorize_pending`) + provider-native metrics and a `plaid_unmapped` coverage stat. Consumer contract (one resolved row/txn) unchanged. The opt-in AI→`provider_native` upgrade pass shipped as `improve-ai` (the immediate follow-up); the per-source candidates view remains **deferred** (with the axis-2 category-seed audit). |
| [Category Source Map](category-source-map.md) | Feature | implemented | M1V — provider-code → canonical-category bridge. Replaces the single `plaid_detailed` tag with `seeds`/`app.category_source_map` → `core.bridge_category_source_map` (VIEW), keyed `(source_type, source_category_code)` → `category_id` (canonical-by-PK — exactly one category per code, deterministic reverse lookup), two-tier detailed+primary via `code_level`, `source_taxonomy_version` drift marker; absorbs any aggregator with no schema change. Adds accounting `class` (income/expense/transfer/debt) to the category dim. Re-derives the seed against Plaid's verified PFC taxonomy (fixes 5 invalid tags, formalizes 16 primary-level rows; 29-code coverage gap → axis-2). Hard-cuts `plaid_detailed`. Prerequisite for the Tier-2b categorizer. |
| [Category Taxonomy Audit](category-taxonomy-audit.md) | Feature | implemented | M1W — the **content** twin of M1V's mapping (axis 2, not axis 1). Audited all 108 seed categories against four principles (earn-the-split granularity, class-by-accounting-nature, no redundant/orphan categories, provider-neutral) → **112-category** set (108 − 5 + 9): retired 5 duplicates/orphans (incl. resolving the two-mortgage ambiguity to `LNP-MTG` debt), added 9 — 6 finer categories from the 29-code triage plus a 3-category **Family & Kids** group (`FAM`/`FAM-ACT`/`FAM-SUP`) folded in after a cross-aggregator comprehensiveness crosswalk (MX/Mint/Monarch/Maybe) validated the set — reconciled `class` (zero reclasses), and hardened the seed-validation test (valid-class invariant + enumerated coverage report + orphan allowlist). Purely additive on the bridge. Internal 4-class scheme only; IRS Schedule C / chart-of-accounts crosswalk deferred to `us_tax` (M2M). |

## Investments

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Overview](investments-overview.md) | Umbrella | draft | Milestone M1J keystone. Four pillars: (A) investment data model, (B) cost-basis & gain/loss engine, (C) price feeds & valuation, (D) net-worth integration. A+B ship first and reach the "ties to a real 1099-B" bar **without** a price feed (realized gain/loss is ledger-derived). This full-tax-year 1099-B tie-out is the M1J close gate; holdings dashboards and price-feed polish do not substitute for it. Fixes the contracts ~6 gated specs wait on (Plaid Investments sync, portfolio/holdings reports, investment matching/transfers, investment OFX, holdings-in-net-worth). Asset/investment dividing line: market ticker → investment. |
| [Investment Data Model & Cost-Basis Engine](investments-data-model.md) | Feature | draft | Foundation child (Pillars A+B). `core.dim_securities` (surrogate `security_id` + resolution chain), `core.fct_investment_transactions` ledger (closed `type` taxonomy), derived `core.fct_investment_lots` / `core.fct_realized_gains` / `core.dim_holdings` (Invariant 8). FIFO + specific-ID + average-cost as elections over one lot grain; manual entry via `raw.investment_transactions`; top-level `investments` CLI/MCP group. Mirrors the broker's method (no IRS-policy enforcement); `currency` column lands now, FX deferred to M1K. Moves the lot-selection override home from `app.us_tax_*` to core `app.lot_selections`. |

## Global Schema

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Multi-Currency](multi-currency.md) | Feature | draft | M1K foundational schema wave, not display polish. **Original currency is canonical at every grain; conversion is presentation-time** (never mutates stored originals). Phased: **M1K.1** capture + integrity + home currency + no-silent-blend guard (independent of investments; may precede the first public release); **M1K.2** display conversion via auditable Frankfurter rates (after investments); **M1K.3** realized FX gain/loss (reuses the investments cost-basis engine). Raw stays source-of-truth; core rebuilt, not patched. Resolves the core currency design questions inline; flags one coherence call (canonical `currency_code` naming). |

## Privacy & Security

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Privacy & AI Trust](privacy-and-ai-trust.md) | Framework | ready | AI data flow tiers, consent model, provider profiles, redaction engine, audit log, and the deterministic-computation / LLM-prose invariant: MoneyBin computes financial truth; AI may explain cited results. |
| [Data Classification](privacy-data-classification.md) | Feature | implemented | `DataClass` registry + PR 2 middleware (introspection, redaction, privacy.log, generic `ResponseEnvelope[T]`, decorator derive-from-return-type, CLI parity). PR 3 shipped the consent ledger (`app.ai_consent_grants` + CLI/MCP grant/revoke/status/log + paired audit; enforcement gate deferred). PR 4 shipped SQL lineage: `sql_query` now resolves each output column's `DataClass` via sqlglot and masks CRITICAL columns (account/routing numbers) — same rules as the typed tools. Consent gate still deferred project-wide. |
| [Data Protection](privacy-data-protection.md) | Feature | implemented | DuckDB encryption at rest (AES-256-GCM), `Database` connection factory, key management, file permissions, PII log sanitization |
| [Privacy & Security Roadmap](privacy-security-roadmap.md) | Roadmap | — | Three-tier data custody model overview |

## MCP

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Architecture & Design](mcp-architecture.md) | Architecture | in-progress | MCP v1 design philosophy, tool taxonomy, privacy integration, CLI symmetry, Apps readiness. Supersedes archived `mcp-read-tools` and `mcp-write-tools` specs. |
| [MoneyBin MCP](moneybin-mcp.md) | Architecture | in-progress | Concrete tool, prompt, resource, and service layer definitions for MCP. v2 (2026-05-02, in-progress) aligns naming with `moneybin-cli.md` v2 taxonomy (path-prefix-verb-suffix), adds `reports_*` namespace, exposes sync + transform to MCP under the v2 exposure principle. Renamed from `mcp-tool-surface.md` (2026-05-16) for symmetry with `moneybin-cli.md` and the future `moneybin-rest-api.md`. |
| [SQL Schema Discoverability](mcp-sql-discoverability.md) | Feature | implemented | `moneybin://schema` resource exposes curated interface tables (core + select app) with columns, comments, and example queries; eliminates per-session schema reconnaissance |
| [Tool Timeouts & Cancellation](mcp-tool-timeouts.md) | Feature | implemented | Global 30s wall-clock cap on every tool dispatch with DuckDB `interrupt()` + connection close on timeout, so a hung call can't wedge the server's write lock |
| [Agent Visualization](agent-visualization.md) | Feature | draft | Chart-ready response projections (long-form contiguous series, top-N + `other` + `share_pct`), presentation hints via tool descriptions + `actions[]`, and a served visualization guide — the model is the renderer in every shipping host, so rendering expertise lives in the responses. Host-agnostic by design; per-host capability matrices are an explicit non-goal. First work item (M3K.1) under the planned `mcp-ux-standards.md` umbrella. Origin: 2026-06-12 MCP-App spike verdict. |
| [First-Run Setup](mcp-first-run-setup.md) | Feature | implemented | M3N. `mcp serve` always boots even with no profile (no more interactive wizard corrupting the JSON-RPC stream with ~8 stdout parse errors). First tool call drives setup: elicitation-capable clients (Claude Desktop) get a one-question `ctx.elicit` for the profile name → `ProfileService.create` → proceed, no restart; tools-only clients get one structured `infra_setup_required` envelope. Only the name crosses the context — the encryption key is generated server-side, so the `profile_*`-is-CLI-only rule holds. New `FirstRunSetupMiddleware` (coherent with `ValidationErrorMiddleware`). |
| `mcp-reports-lineage.md` | Feature | planned | "Show me the SQL" surface: report responses expose a stable `result_ref` / `lineage_ref`; primary MCP tool is `reports_explain(result_ref=...)`, with `reports_explain_last` only as convenience. Returns params, rendered SQL, SQLMesh model/view path, output column privacy classes, source tables/views, and safe provenance queries/counts by default. Tool-only by design — keeps the normal response envelope clean and stays portable across tools-only clients. Origin: internal roadmap review 2026-05-16; promoted to durable result-ref contract 2026-05-28. |
| `llm-prose-summaries.md` | Feature | planned | Post-launch AI explanation surface. Deterministic MoneyBin services/reports compute every number and classification; LLMs may only write prose summaries from cited `result_ref` / report outputs. Requires redaction/consent boundaries, provider/local-model docs, audit logging, and UI/MCP copy that labels prose as explanation rather than source of truth. Not an M1/M2/M3 blocker. |

## Sync

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Overview](sync-overview.md) | Umbrella | in-progress | Provider-agnostic sync framework: interaction model, SyncClient, CLI/MCP surface, E2E encryption design, provider contract. Phase 1 shipped via `sync-plaid.md`; Phase 2 scheduling unstarted; Phases 3-4 (E2E + post-quantum) remain design sketches. Supersedes archived `sync-client-integration` spec. |
| [Plaid Provider](sync-plaid.md) | Feature | implemented | First sync provider: Plaid Transactions. Raw schemas, staging views, core integration, Plaid Hosted Link flow, error codes. M1G Phase 1 shipped. |
| `sync-simplefin.md` | Feature | planned | SimpleFIN aggregator provider (alternative to Plaid). **Deferred 2026-05-16** (internal roadmap review): no implementation work until M1G produces 30 days of real Plaid data from a real user — provider-ladder elaboration ahead of real user evidence is speculative. Spec may be drafted earlier; engineering work waits for the gate. |
| `sync-plaid-investments.md` | Feature | planned | Plaid Investments product (gated on `investments-data-model.md`) |

## Connect (Live External Sources)

Client-direct OAuth to user-owned data sources. Distinct from `sync` (which mediates third-party financial providers via moneybin-sync): `connect-*` providers are user-controlled storage that the MoneyBin client speaks to directly.

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Google Sheets](connect-gsheet.md) | Feature | implemented | Live tabular sync from Google Sheets: OAuth user-flow (PKCE), pre-refresh hook + explicit pull, live mirror with soft-delete, stable-key detection for edit-preserving identity, strict drift refusal per-connection. v1 ships two adapters: `transactions` (Tiller-style integrated path → matching/categorization/reports) and `seed` (catch-all escape hatch → JSON storage + auto-generated typed views queryable via SQL/MCP). New `app.gsheet_connections` + `raw.gsheet_seeds` tables; one new column (`deleted_from_source_at`) on `raw.tabular_transactions`. M1F entry. |
| `connect-airtable.md` | Feature | planned | Future sibling — Airtable as a live tabular source. Shares the connection lifecycle pattern from `connect-gsheet.md`. |
| `connect-smartsheet.md` | Feature | planned | Future sibling — Smartsheet as a live tabular source. |

## Testing & Validation

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Overview](testing-overview.md) | Umbrella | ready | Verification infrastructure: synthetic data, assertions, scenarios, format/migration testing |
| [Synthetic Data Generator](testing-synthetic-data.md) | Feature | implemented | Persona-based synthetic financial data: YAML-driven personas/merchants, deterministic seeding, ground-truth labels, Level 2 realism |
| [E2E Testing](testing-e2e.md) | Feature | implemented | Subprocess-based E2E tests: smoke tests (help, no-DB, DB commands), golden-path workflow tests (synthetic, CSV, OFX, lock/unlock, categorization) |
| [Scenario Runner](testing-scenario-runner.md) | Feature | implemented | Whole-pipeline correctness: empty DB → pipeline → assertions/expectations/evaluations against synthetic ground truth and hand-labeled fixtures; scenario suite (`make test-scenarios`); validation primitives reusable for live-data checks |
| [Normalize-Description Fixtures](testing-normalize-description-fixtures.md) | Feature | implemented | YAML golden cases for `normalize_description()`; parametrized exact-equality tests; contributor-facing surface for adding real-world transaction descriptions |
| [Comprehensive Scenario Testing](testing-scenario-comprehensive.md) | Feature | implemented | Five-tier assertion taxonomy, independent-expectations rule, bug-report recipe, relocation of scenarios to `tests/scenarios/`. Architectural authority for all future scenario work. |
| [Anonymized Data Generator](testing-anonymized-data.md) | Feature | draft | Structure-preserving anonymization of real databases for shipping bug-report fixtures and format seeds without leaking PII; statistical similarity guarantees. M1 (Ingestion Core) — the real-data parity enabler for the Ingestion-Complete gate. |
| `testing-csv-fixtures.md` | Feature | planned | Curated bank export samples with expected-result YAML for format detection testing |
| `testing-format-compat.md` | Feature | planned | Extractor verification against fixture files |
| `testing-migration-safety.md` | Feature | planned | Pre/post migration data integrity assertions |

## Infrastructure

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Shared Primitives](architecture-shared-primitives.md) | Architecture | implemented | Reference doc for the 12 primitives that crystallized through M0 — `Database`, `SecretStore`, service-layer contract, `TableRef`, `ResponseEnvelope`, `@mcp_tool`/privacy middleware, `@tracked`/`track_duration`, `SanitizedLogFormatter`, `ingest_dataframe`, `MoneyBinSettings`, SQLMesh layer conventions, scenario fixture format. Names the `app.*` user-state layer and the new `reports.*` presentation layer; cited by every later spec. |
| [Source Observations](source-observations.md) | Architecture | implemented | Positioning doc (no new primitives). Names canonical homes for per-source factual claims: transactions via `raw.*` + `meta.fct_transaction_provenance` + `app.match_decisions`; balances via `core.fct_balances` + `app.balance_assertions` + `core.fct_balances_daily`; non-canonical payloads via `raw.pdf_seeds`. Forbids parallel `core.fct_source_observations` / `core.bridge_transaction_observations` / `app.observations`. Gives the Web UI a read map so it does not re-derive observations as app-state. Three genuine gaps tracked as followups (Plaid balance sync, anchor-to-anchor drift audit, PDF balance summaries). M1M. |
| [Extension Contracts](extension-contracts.md) | Architecture | in-progress | Contributor-facing surface for the three extension types — Reports, Analysis Packages, Providers — with distinct trust postures (open ecosystem for reports/packages with capability-declared constraints; in-tree only for providers). Intentionally narrower than a broad plugin SDK: no arbitrary UI plugins in 1.0; typed UI/MCP App component slots wait until the Web UI and hosted runtime mature. Pluggable via setuptools entry points; framework-validated prefix discipline; Quality Scale (bronze → platinum) cross-cutting all three. 1.0 reference packages: `assets` + `us_tax`; all in-tree extensions ship at Platinum. Four `/moneybin-*` guided-contribution skills + `moneybin extension validate` shipped with the Claude Code plugin. The framework (M1Q) locks at the M1→M2 boundary; reference packages are M2M, contributor UX is M3I. |
| [UI Architecture](ui-architecture.md) | Architecture | draft | Shared frontend foundation for both visual surfaces — the MCP App (`apps/mcp-app`) and the Web UI (`apps/web`) — as two thin shells over one shared `ui-core` (React + shadcn/Tailwind/Tremor). Transport-agnostic data layer: components consume an injected `MoneyBinClient` via TanStack Query hooks; `transport-mcp` (MCP Apps postMessage/JSON-RPC) and `transport-http` (fetch→FastAPI) implement it. The discipline contract (`ui-core` depends on neither transport) is structural, not conventional. Types codegen from Pydantic JSON Schema; React bundle builds once and embeds in the Python wheel (users see no Node). Stubs extension UI slots; documents in-app AI as a non-goal but forward-compatible render target. Establishes the pattern in [ADR-014](../decisions/014-shared-ui-architecture.md). Foundation for `web-ui-prototype.md` + the two `*-dashboard-mcp-app.md` consumer specs. M3L (proposed). |
| `self-host-headless-operations.md` | Feature | planned | Post-`moneybin-sync` design batch. Covers any build specs needed for self-host/headless operation plus the operator guides: profile/data paths, backup/restore verification, cron/systemd scheduled imports/sync, MCP setup, logs, key management, Docker Compose/env reference for `moneybin-sync`, OIDC/billing/object-store choices, and security caveats. Gated on `moneybin-sync` completion; no supported self-host server promise before the runtime exists. |
| [MoneyBin CLI](moneybin-cli.md) | Architecture | in-progress | Target command taxonomy across CLI / MCP / future HTTP. v1 implemented; v2 (2026-05-02, in-progress) dissolves `track`, introduces entity groups (`accounts`, `transactions`), adds `categories`, `merchants`, `assets`, `reports`, `system` top-level groups, separates `tax`, codifies MCP exposure principle, renames MCP tools. Reference spec for all other specs' surface placement. Renamed from `cli-restructure.md` (2026-05-16) for symmetry with `moneybin-mcp.md` and the future `moneybin-rest-api.md`. |
| [Core `updated_at` Convention](core-updated-at-convention.md) | Architecture | implemented | Row-level freshness contract for `core.*` models (`updated_at` = MAX of per-row inputs; NULL where all inputs are model-level seeds) and model-level freshness via `meta.model_freshness` (wraps SQLMesh `_snapshots`). Adds `updated_at` to `app.user_categories`, `app.user_merchants`, `app.category_overrides`; replaces misleading `CURRENT_TIMESTAMP AS updated_at` on `dim_accounts`. |
| [Observability](observability.md) | Feature | implemented | Logging consolidation, `prometheus_client` metrics with DuckDB persistence, instrumentation API (`@tracked`, `track_duration`), log/stats CLI commands |
| [Database Migration](database-migration.md) | Feature | implemented | Dual-path schema migration system: auto-upgrade on first invocation, SQL/Python migrations, rebaseline, SQLMesh version detection |
| [Database Writer Coordination](database-writer-coordination.md) | Feature | implemented | Implement ADR-010: replace long-lived read-write singleton with short-lived purpose-declared connections; `read_only` flag on `Database`; `get_database()` retry; multi-process E2E tests |
| [`app.*` Integrity Invariant](app-integrity-invariant.md) | Feature | ready | Invariant 10 (`app.*` mutation routing): every protected `app.*` mutation emits a paired `app.audit_log` row inside the same DuckDB transaction. Repository pattern (`*Repo` classes in `src/moneybin/repositories/`) makes audit structural rather than disciplinary; lint rule blocks raw `INSERT/UPDATE/DELETE` against protected tables outside `*_repo.py`/`audit_service.py`; doctor adds per-table audit-coverage + FK invariants. Reversibility contract (full `before_value`, threaded `parent_audit_id`) bakes in Phase 1 to keep Phase 2 undo strictly additive. Gates hosted (M3H). |
| [User-Facing Doc Polish](user-facing-doc-polish.md) | Feature | in-progress | README rewrite, CHANGELOG backfill, threat-model guide, License section with substance, expanded comparison table, milestone-aligned roadmap. Tagline preserved; honesty layer below. "Now" batch shipped; M0D (architecture distillation) and M3B (brew install + demo asset) remain. Those docs should surface earned trust signals only when real: doctor, scenarios, CI/security, privacy/redaction, audit/undo. |
| `export.md` | Feature | planned | Plaintext export / walk-away contract. Local files are the canonical contract: `moneybin export bundle` writes canonical `core.*`, selected `reports.*`, and user-authored `app.*` curation state to CSV/Excel with a manifest (`moneybin_version`, schema/profile metadata, table list, row counts, checksums, privacy mode) and a generated data dictionary. Default export excludes raw rows and sensitive identifiers unless explicitly requested. Google Sheets is a v1 target candidate built on the same bundle contract (`--target gsheet`), leveraging existing gsheet OAuth/API code but gated on write-scope consent, new-workbook defaults, and manifest-protected overwrite semantics. |
| `cli-ux-standards.md` | Architecture | planned | CLI interaction patterns: progressive disclosure, review queues, status commands, output formatting |
| `mcp-ux-standards.md` | Architecture | planned | MCP interaction patterns: tool naming, error surfaces, prompt design, resource conventions |

## Data Quality

| Spec | Type | Status | Summary |
|---|---|---|---|
| [MoneyBin Doctor](moneybin-doctor.md) | Feature | implemented | Pipeline integrity command: `moneybin system doctor` runs SQLMesh named audits (FK integrity, sign convention, balanced transfers) + staging coverage + categorization coverage warning. Produces an "N invariants passing" trust artifact. CLI lives under the `system` group; MCP exposes `system_doctor`. Anchor for the broader quality-gate trust surface. |
| [Data Pipeline Reconciliation](data-reconciliation.md) | Feature | draft | Broader ETL integrity checks: raw→prep→core row accounting, import batch validation, temporal coverage gaps, orphan detection. `moneybin-doctor.md` is the user-facing subset; this spec covers the full warehouse-grade reconciliation surface. |
| [Recoverable State Contract](data-recovery-contract.md) | Feature | in-progress | M1L milestone. Universal `recovery_actions` envelope on every error and audit failure; Invariant 10 Phase 2 audit-log undo (`system_audit_undo` / `_history` / `_get`) with `operation_id` grouping; doctor recipe registry; 5-recipe self-heal safelist gated on five trust criteria; matches MCP surface closing the CLI-only gap; `RefreshResult` surfacing matcher/categorizer crashes that currently log at DEBUG. Adds Invariant 11 (Recoverability of mutations) and new always-loaded rule `.claude/rules/data-recovery.md`. |

## Reports & Health

| Spec | Type | Status | Summary |
|---|---|---|---|
| [Reports Recipe Library](reports-recipe-library.md) | Feature | implemented | Eight `reports.*` SQLMesh views (`net_worth`, `cash_flow`, `spending_trend`, `recurring_subscriptions`, `uncategorized_queue`, `merchant_activity`, `large_transactions`, `balance_drift`) inaugurating the `reports` schema; one-time migration of `core.agg_net_worth → reports.net_worth`; CLI/MCP `reports_*` surfaces extended; `moneybin://schema` discoverability extended. M2A entry. |
| `reports-anomaly-detection.md` | Feature | planned | Anomaly detection as a real tool (`reports_anomalies` or similar), not an MCP prompt: compare a window to a trailing-N-month baseline and surface deltas exceeding a threshold. Tool-first per internal roadmap review 2026-05-16 — replaces the earlier "monthly-ritual MCP prompt set" plan. Can be wrapped as a thin MCP prompt later. |

## Standalone
| [Account Management](account-management.md) | Feature | implemented | Owns the `accounts` entity namespace: list/get/set/resolve, reversible account merging via bridge model, per-account settings (`app.account_settings`), display preferences. CLI per `moneybin-cli.md` v2: top-level `accounts` (entity ops; balance lives nested under `accounts balance` per `reports-net-worth.md`). The unified `accounts set` is the partial-update entry point for every settings field, including the behavioral flags (display name, include/exclude, archive/unarchive) that were previously separate verbs. Bundles with `reports-net-worth.md`. |
| [Account Identity Resolution](account-identity-resolution.md) | Feature | in-progress | M1S. One real account = one canonical, **opaque non-PII** `account_id` across OFX/CSV/PDF/Plaid, via a resolution step every import/sync runs through. **M1S.7–.9 (live-test reconciliation, Decision 8):** cross-source linking didn't fire in the wild — last4 is never *derived* into `dim_accounts` (only user-set lands), mutable CSV labels are keyed as hard refs (rename → duplicate), and the exporter/tool name is used as the account institution (aggregator bridge dead). Fix: A/B/C signal tiers, capture-into-`dim` + per-source capture contract, CSV bind-first with format/account decoupling, exporter↔institution split. **Two `app.*` tables** mirroring the matcher: `account_links` (native_ref→canonical mapping) + `account_link_decisions` (proposal/review queue); `dim_accounts` re-keyed on the surrogate with COALESCE merge. Ladder: remembered ref / scoped full-number / persistent-token → auto-adopt; `institution+last4`/name → review (never auto-merge). Import-time **detect→confirm→remember** (extends `import_confirm` with a per-account binding map); surfaces `accounts_links_*` + top-level `review`. Transaction identity stays **content-derived + alias forwarding**, not a surrogate ([ADR-015](../decisions/015-transaction-identity-content-derived.md)). **Unblocks** cross-source txn dedup (`matching-exact-key-dedup.md`, inert today because `fct.account_id` differs per source) + deferred account merge. Fixes the verified 2026-06-13 finding: 5 WF accounts × (qfx+csv) → 558 rows / 10 ids instead of 279 / 5. |
| [Net Worth & Balance Tracking](reports-net-worth.md) | Feature | implemented | Authoritative balance tracking per account, daily carry-forward interpolation, reconciliation deltas, `agg_net_worth` aggregation; cash-only v1. CLI per `moneybin-cli.md` v2: `accounts balance` (per-account workflow), `reports networth` (cross-domain rollup). Bundles with `account-management.md` (shared `accounts` namespace + `app.account_settings`). |
| [Asset Tracking](asset-tracking.md) | Feature | draft | Physical asset registry (real estate, vehicles, valuables) with periodic valuations, staleness warnings, liability linking, and net worth integration. CLI namespace: top-level `assets` per `moneybin-cli.md` v2 (parallel to `accounts`). |
| [Budget Tracking](budget-tracking.md) | Feature | draft | Monthly budgets with target-vs-actual and rollovers. CLI namespace: top-level `budget` (mutation) + `reports budget` (vs-actual) per `moneybin-cli.md` v2. |
| `recurring-transactions.md` | Feature | planned | Storage table for recurring/scheduled transaction definitions (Netflix, rent, utilities) with expected amount bands (min/max/avg) and predicted next-occurrence dates. Storage-first; auto-detection deferred. Sure-shape reference (`we-promise/sure: db/schema.rb` `recurring_transactions`). Surfaces "did I get charged twice?" / "is this bill higher than usual?" / cashflow forecasting inputs. |
| `account-subtype-detail.md` | Feature | planned | Subtype-validated account fields (credit limits + payoff dates on `CreditCard`; cash/non-cash split on `Investment`; appraisal data on `Property`; vehicle metadata). Extends `account-management.md` (shipped) with subtype-aware schema; today's `app.account_settings` carries flat fields without subtype validation. Sure-shape reference (`accountable_type` polymorphism, lifted as MoneyBin sibling tables). |
| [Transaction Curation](transaction-curation.md) | Feature | implemented | User-state layer on transactions: manual entry (CLI single, MCP bulk), multi-note threads, multi-tag, split-via-annotation, import-batch labels, unified `app.audit_log` (subsumes `app.ai_audit_log`). New `app.*` curation tables; `core.fct_transactions` extended with LIST/STRUCT presentation; new `core.fct_transaction_lines` view. Establishes CLI-imperative / MCP-declarative-set pattern. M1E entry spec. |
| `web-ui-prototype.md` | Feature | planned | M3A/M3C first Web UI surface (FastAPI + React): complete app shell with deep trust/review workflows. Navigation covers Overview, Transactions, Review, Reports, Accounts, Imports, Data Quality, Export, Settings. V1 depth focuses on categorization/import review, doctor/invariants, transaction inspect/search, report results + lineage handoff, export bundle, and Data Quality trust signals for scenarios/CI/privacy/audit/undo. Domain-specific UIs (budgets, investments, tax, loans, goals, household, mobile polish) wait until the backing domain exists. Stress-tests the writer-coordination contract from `architecture-shared-primitives.md` under real concurrent reads/writes. |
