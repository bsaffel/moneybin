Guided review now previews the exact limited match selection before confirming it and refuses stale selections before writing decisions.

Import receipts now retain needs-confirmation work, distinguish it from failures, and exit nonzero until the required confirmation is supplied.

Mutation confirmation now requires an interactive terminal with visible output; JSON and piped input cannot authorize note, split, rule-conflict, or match changes without an explicit confirmation flag.
