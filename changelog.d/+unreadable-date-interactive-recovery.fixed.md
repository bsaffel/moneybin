`moneybin import files` stopping because the mapped date column could not be parsed now prints the same recovery at an interactive terminal that it already returned under `--output json`: name the real date column with `--mapping transaction_date=<source_column>`, or declare its layout with `--date-format`. The interactive output previously suggested `--confirm` and `moneybin import confirm --accept`, which ratify the same unreadable mapping and raise the refusal again, alongside a `--mapping description=<column>` hint naming the wrong field.
