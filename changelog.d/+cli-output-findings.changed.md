CLI next-step hints are now executable `moneybin` commands on every report and on `system doctor`; receipts label money with its currency, right-align and group every numeric column, never split a path or timestamp across lines, say when a rebuild is needed to see a change in reports, and an empty report names the scope it searched. MCP `import_files` and `import_confirm` now report an assumed sign convention per file (`sign_assumed`) with revert-first recovery, matching the CLI's attention line.
