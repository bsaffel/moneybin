Encryption-key environment fallback is now profile-scoped: rename existing exports to `MONEYBIN_PROFILE__<PROFILE>__DATABASE__ENCRYPTION_KEY` (uppercase the normalized profile name and replace hyphens with underscores). Sync login and refresh require writable keychain storage; existing profile-specific plaintext token files are removed only after both credentials are stored and verified in the keychain.
