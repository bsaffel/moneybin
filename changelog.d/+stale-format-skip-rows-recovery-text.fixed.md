An import that stops because a real transaction was consumed as the header row now prints recoveries that exist — re-run without `--format` so the layout is detected fresh from the file, and `moneybin import formats delete <name> --yes` to remove a saved format that no longer matches it — instead of advice to add a header row the file already has. The printed retry repeats the read options the failed read actually resolved rather than only the flags you typed, so a sheet, delimiter, encoding, date format or number format the saved format supplied survives dropping `--format`; the same corrected text now also reaches interactive terminals and `moneybin import preview`, which previously printed advice that re-applied the stale `--format` and re-raised the refusal, and an inbox sidecar now warns that this recovery leaves the file in `pending/`.
