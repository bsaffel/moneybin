A headerless Excel (`.xlsx`) import no longer loses its first transaction. The Excel reader now detects a missing header row on the same terms as the CSV and Parquet readers instead of always treating row 0 as column names, so a headerless sheet previews with `has_header: false` and keeps every row — the format a file is saved in no longer changes whether a record survives the import.
