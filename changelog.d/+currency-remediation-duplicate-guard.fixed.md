`system doctor`'s currency-integrity check now warns when an unknown-currency account mirrors an existing account's transactions, and directs you to resolve account identity before assigning a currency. Declaring a pair genuinely distinct with `accounts links set <decision_id> --standalone` lifts the withheld advice for that pair, so the check does not keep demanding a decision you already made.
