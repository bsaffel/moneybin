Two overlapping opens of the same database from one process no longer fail. DuckDB refuses to attach a file that another connection in the same process still holds, and MoneyBin surfaced that as `Binder Error: Unique file handle conflict` instead of waiting its turn. It now retries like any other lock contention, so two reads that both find the schema behind now both complete, one after the other.
