`moneybin import preview` now accepts `--date-format` and `--number-format`, so it reads a file the same way the `import files` or `import confirm` invocation it previews will. Previously a file whose dates needed a declared format was previewed without it, reporting a header row and one transaction fewer than the import itself would load — including when the preview command came from MoneyBin's own printed recovery hint.
