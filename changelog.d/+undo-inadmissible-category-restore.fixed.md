`system_audit_undo` / `moneybin system audit undo` now refuses (`undo_value_inadmissible`) to restore a category or subcategory value the write path no longer admits, such as a blank string predating the earlier whitespace-only rejection, instead of silently writing it back on `app.user_categories`, `app.user_merchants`, `app.transaction_splits`, `app.budgets`, `app.categorization_rules`, `app.proposed_rules`, `app.transaction_categories`, `app.rule_conflicts`, and `app.categorization_decisions`.
