A report column can now declare the currency it is already denominated in through `OutputColumn.currency_basis`, so display conversion prices each money column from its declared basis instead of assuming the row's own currency. A column a model had already converted to the home currency is no longer converted a second time; every existing column keeps its current behavior by default.
