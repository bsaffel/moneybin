`raw.ofx_institutions` is gone from `moneybin sql query` / the `sql_query` MCP tool. Nothing read it — institution org and FID have always been duplicated on `raw.ofx_accounts` — and its narrow primary key (`organization`, `fid`, with no `source_file`) meant reverting one OFX import could silently delete an institution row a different, still-complete import needed. Query `SELECT DISTINCT institution_org, institution_fid FROM raw.ofx_accounts` for the same data.
