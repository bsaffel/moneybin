Investment imports preserve source revisions and delivery order, including the first holdings snapshot for each account. `investments add` and `investments_record` no longer accept caller-authored grouping and write each reinvestment group atomically; overlap warnings now include holdings-only syncs while preserving existing withholding and opening-bootstrap exclusions.
