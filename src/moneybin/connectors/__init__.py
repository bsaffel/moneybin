"""Client-side connectors for external sync services.

This module contains connectors that interface with hosted sync services
like MoneyBin Sync (for Plaid) and other bank aggregation services.
These connectors delegate to server-side APIs (or local server code in dev mode).
"""
