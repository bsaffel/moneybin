"""Analysis package namespace.

Reference packages (assets, us_tax — Plan 4) install as siblings here at
distribution install time via entry-points. The framework that discovers and
validates packages lives under _framework/.
"""
